## Summary



## Test plan

<!--
Every item below needs enough detail that someone other than the
author — or the author, a week later — could execute it without
guessing. That means, per item:
  - the exact steps (what to click, type, or run)
  - the exact place to look (a file, a devtools panel, terminal output)
  - the exact expected result (not just "it works")

Bad:  - [ ] Tested export/delete
Good: - [ ] **Not verified**: live delete/export in a real browser (no
        browser in this environment). Start capture, speak briefly,
        Stop. Hit Export and confirm the downloaded JSON has the
        session with its frames. Hit Delete all, confirm the dialog,
        then check devtools → Application → IndexedDB →
        `resonance-scope` is empty.

If a check can't be run in this environment (no browser, no real
microphone, etc.), say so explicitly and leave the box unchecked
rather than skipping it — that's the signal it still needs a human
pass before merge.
-->

- [ ] `npm run lint` / `typecheck` / `test` / `build` — all clean
- [ ]
